class Algorithm {
  constructor(name, learn, classify, convert, convertBatch, generateInterface) {
    this.name = name;
    this.learn = learn;
    this.classify = classify;
    this.convert = convert;
    this.convertBatch = convertBatch;
    this.generateInterface = generateInterface;
  }
};
